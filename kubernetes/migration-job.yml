apiVersion: batch/v1
kind: Job
metadata:
  name: db-migration
  namespace: ecommerce
spec:
  template:
    spec:
      containers:
      - name: migration
        image: kmvishal/easyshop-migration:latest
        imagePullPolicy: Always
        env:
        - name: MONGODB_URI
          value: "mongodb://mongodb-service:27017/easyshop"
      restartPolicy: OnFailure